import './globals.css';
export const metadata = { title: 'Microverse', description: 'Создай игру за 60 секунд' };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ru">
      <body className="bg-cosmic-black text-white">{children}</body>
    </html>
  );
}
